# Sudoku-solver
python code to solve a sudoku | uses brute force backtracking to achieve the specific target number | uses python list to store the table and fixed numbers are sting while non fixed number are integers | CLI 
